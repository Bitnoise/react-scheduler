import styled from "styled-components";

export const StyledWrapper = styled.div`
  position: absolute;
  height: min-content;
  width: min-content;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
`;
