import { SchedulerRowLabel } from "@/types/global";

export type LeftColumnItemProps = {
  item: SchedulerRowLabel;
  rows: number;
};

export type StyledTextProps = {
  isMain?: boolean;
};

export type StyledLeftColumnItemWrapperProps = {
  rows: number;
};
